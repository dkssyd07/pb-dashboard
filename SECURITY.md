# 보안 정책 (Security Policy)

## 지원 버전 (Supported Versions)

| 버전 | 보안 지원 여부 |
|------|--------------|
| 1.x (최신) | ✅ 지원 |

## 보안 취약점 신고 (Reporting a Vulnerability)

보안 취약점을 발견하셨다면 **공개 이슈(Issue)를 통한 신고를 지양**해 주세요.  
아래 방법으로 비공개 제보해 주시면 신속하게 검토하겠습니다.

**신고 방법:** 이메일 또는 GitHub Private Disclosure 기능 사용

보고서에는 다음 내용을 포함해 주세요:
- 취약점 유형 및 영향 범위
- 재현 방법 (단계별 설명)
- 개념 증명(PoC) 코드 또는 스크린샷 (가능한 경우)
- 예상 영향도

신고 접수 후 **7일 이내** 확인 응답을 드리며, 수정 완료 시 제보자를 명시합니다 (원하시는 경우).

## 보안 고려사항 (Security Considerations)

### API 키 관리
- **Anthropic API 키**는 클라이언트 측(브라우저)에서 직접 입력하여 사용됩니다.
- API 키는 브라우저 메모리에만 유지되며, 서버 또는 외부로 전송되지 않습니다.
- API 키를 소스 코드나 Git에 하드코딩하지 마십시오.
- 공용 또는 공유 환경에서 사용 후 키를 반드시 삭제하십시오.

### 데이터 처리
- 업로드된 PDF 리포트는 브라우저 내에서만 처리됩니다.
- 고객 정보 또는 민감한 투자 데이터를 외부 API로 전송 시 유의하십시오.
- 실시간 시세 API 연동 시 API 키를 환경 변수(`.env`)로 관리하고, `.gitignore`에 포함하십시오.

### 클라이언트 사이드 보안
- 이 프로젝트는 정적 HTML/CSS/JS로 구성되어 서버 측 취약점은 없으나, XSS 방어를 위해 동적 콘텐츠 삽입 시 DOM 조작 방식에 주의하십시오.
- `innerHTML` 사용 시 신뢰할 수 없는 데이터는 반드시 이스케이프 처리하십시오.

## 면책 조항 (Disclaimer)

이 소프트웨어는 투자 정보 제공 목적으로 제작되었으며, 투자 판단의 최종 책임은 사용자에게 있습니다.  
실제 투자 결정은 반드시 전문 PB 및 금융 전문가의 판단을 종합하여 이루어져야 합니다.
